{
  "buildCommand": "ROLLUP_WASM=1 npm run build",
  "devCommand": "npm run dev",
  "installCommand": "ROLLUP_WASM=1 npm install",
  "builds": [
    {
      "src": "nuxt.config.ts",
      "use": "@nuxtjs/vercel-builder"
    }
  ]
}
